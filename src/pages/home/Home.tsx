import Banner from '@/components/Banner';
import Container from '@/components/Container';

const Home = () => {
    return (
        <Container>
            <Banner />
        </Container>
    );
};

export default Home;
