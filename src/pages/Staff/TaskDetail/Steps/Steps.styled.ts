import { Steps } from 'antd';
import styled from 'styled-components';

export const StepsStyled = styled(Steps)`
    margin-top: 16px;

    & .ant-btn {
        margin-left: 4px;
        border-radius: 4px;
    }
`;
